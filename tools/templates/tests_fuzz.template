#![no_main]

use libfuzzer_sys::fuzz_target;
use techalib::{indicators::${indicator_name}::${indicator_name}, types::Float};

fuzz_target!(|data: (...)| {
    // TODO: INPUT ARGUMENTS
    // let (...) = data;
    // let ARG = (ARG as usize % DATA.len().saturating_add(1)).max(1);
    // ...
    // let _ = ${indicator_name}(&DATA, ...);
});
